{
  "_comment": "Source-of-truth for prepare-deploy.sh. Pinned versions of the cross-repo workspace packages that must be resolved from the npm registry during a theokit-packs deploy build. Update this file when the corresponding theokit-sdk publish lands. Verified versions correspond to the dist-tag 'latest' on npm as of 2026-06-01.",
  "@usetheo/sdk": "^1.3.0",
  "@usetheo/gateway": "^0.4.0",
  "@usetheo/gateway-telegram": "^0.1.0"
}
