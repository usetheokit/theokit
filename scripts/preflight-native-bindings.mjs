/**
 * Preflight native bindings — stub.
 * Original implementation was removed during stale docs cleanup.
 * This stub ensures vitest globalSetup doesn't crash.
 */
export async function ensureNativeBindings() {
  // No-op — native bindings checked at install time via pnpm rebuild
}
