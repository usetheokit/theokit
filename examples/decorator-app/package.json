{
  "name": "theokit-decorator-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TheoKit example app with NestJS-style decorators — the result of `npx create-theokit my-app`",
  "scripts": {
    "dev": "node --import ./register.mjs server/main.mjs",
    "test": "echo 'Run from workspace root: pnpm --filter theokit-decorator-app test'"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2",
    "zod": "^3.25.0"
  }
}
