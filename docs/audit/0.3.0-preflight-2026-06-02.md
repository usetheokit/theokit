# 0.3.0 Cutover — Pre-flight Verification (T0.1)

**Date:** 2026-06-02
**Branch:** develop
**Task:** T0.1 from `.claude/knowledge-base/plans/theokit-0-3-0-enforcement-cutover-plan.md` v1.1
**Purpose:** Confirm shipped engineering matches what the cutover plan assumes — surface any drift before authoring 0.3.0 docs against stale code.

## Invariants verified

### Schema defaults (`packages/theo/src/config/schema.ts`)

| Line | Source | Expected | Found | Status |
|---|---|---|---|---|
| 125 | `cspMode: z.enum(['enforce', 'report-only', 'off']).default('enforce')` | exact | exact | ✅ |
| 191 | `csrf: z.enum(['off', 'warn', 'strict']).default('strict')` | exact | exact | ✅ |

The `csrf` enum INCLUDES `'warn'` value — D1 contract holds (no new field needed; existing enum value IS the migration opt-out).

### CSRF multi-header chain order (`packages/theo/src/server/security/csrf-multi-header.ts`)

| Line | Behavior | Status |
|---|---|---|
| 77 | reads `sec-fetch-site` header (Sec-Fetch-Site is primary signal) | ✅ |
| 79 | `'same-origin'` OR `'none'` → pass | ✅ |
| 82-88 | `'same-site'` / `'cross-site'` → reject + log via `limitUntrustedHeaderValueForLogs` | ✅ |
| 96 | Origin fallback via `originMatches(rawOrigin, ownOrigin, options.allowedOrigins)` | ✅ |
| 109 | Referer fallback via `originMatches(refererOrigin, ownOrigin, options.allowedOrigins)` | ✅ |
| 24 | `allowedOrigins?: readonly string[]` wildcard config | ✅ |
| 31 | `trustForwardedHeaders?: boolean` config (EC-5 from blueprint Q1) | ✅ |

Chain order intact — Sec-Fetch-Site → Origin → Referer. T1.1 doc + T3.2 blog post can reference this order as canonical.

### Scanner public surface (`packages/theo/src/cli/commands/upgrade-readiness.ts`)

| Line | Export | Status |
|---|---|---|
| 30 | `export type ViolationRule` | ✅ |
| 37 | `export interface Violation` | ✅ |
| 45 | `export type ReadinessStatus` | ✅ |
| 51 | `export interface UpgradeReadinessReport` | ✅ |
| 57 | `export interface ScanOptions` | ✅ |
| 291 | `export async function scanUpgradeReadiness(options: ScanOptions): Promise<UpgradeReadinessReport>` | ✅ |
| 348 | `export async function upgradeReadinessCommand({...})` | ✅ |

T1.3 (URL emit on success path) will append at L375+ inside `upgradeReadinessCommand` per the plan's `Deep Dives` section. No signature change.

### CSRF/CSP test suite (HEAD, 2026-06-02)

```
✓ tests/unit/csrf-warn-first.test.ts          (15 tests)
✓ tests/unit/csrf-multi-header.test.ts        (17 tests)
✓ tests/unit/csp-report.test.ts               (13 tests)
✓ tests/unit/csrf-readiness-endpoint.test.ts  (7 tests)
✓ tests/unit/csrf.test.ts                     (5 tests)
✓ tests/unit/csrf-disallowed-routes.test.ts   (counted: implicit in pass tally)
✓ tests/unit/csrf-readiness-store.test.ts     (counted)

Test Files  7 passed (7)
Tests       79 passed (79)
Type Errors no errors
Duration    438ms
```

Plus orthogonal devtools suite (T0.1 spot-check confirmed earlier in this session): `176/176 devtools tests green`.

## Verdict

✅ **PASS — zero engineering drift detected.** All assumptions made by the cutover plan v1.1 hold at HEAD. Phases 1-3 are unblocked.

## Followups noted (not blocking)

- T0.1 does NOT verify that `csrf-multi-header.ts` is wired into the request lifecycle of action-execute / route-execute (per edge-case review EC-8 SHOULD TEST). The implicit wiring evidence is the dogfood-app `/api/memory POST` round-trip working today (Chrome MCP verified `saveMemory success 71ms` in Actions tab post-fix `3548d60`). Treating this as "wiring proven by integration test in T2.1" — that spec MUST exercise a real POST that triggers the multi-header chain.

## Next

T0.1 → completed. Unblocks T1.1, T1.2, T1.3, T2.1, T3.1, T3.2 (Phases 1-3 parallel) and T4.4 (rollback runbook, must land before T4.1 publish).
