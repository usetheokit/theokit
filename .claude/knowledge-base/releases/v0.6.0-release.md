# Release v0.6.0

**Date:** 2026-06-17
**Verdict:** PR_OPEN_AWAITING_APPROVAL
**Source review:** `.claude/knowledge-base/reviews/crossval-native-routing-web-fixes-review-2026-06-16.md` (READY_TO_MERGE)
**PR:** https://github.com/usetheodev/theokit/pull/8
**Merge commit:** (pending human merge)
**Tag:** v0.6.0 (cut post-merge, annotated, pointing at the merge commit)
**GitHub release:** (created post-merge)

## Context

First `develop → main` release PR — `main` was 471 commits behind. Workspace tag
cut as **v0.6.0** (aligned with the flagship `theokit@0.6.0`; 0.x retained by
decision — `compute_next_version` derived v1.0.0 from breaking changes, overridden
to v0.6.0 to avoid a premature 1.0 stability claim).

## Already published to npm (ahead of this PR — validated end-to-end)

- `theokit@0.6.0`
- `create-theokit@1.0.15`

Validated against the published artifacts: `npx create-theokit@1.0.15` → `npm install`
(no ERESOLVE) → `theokit dev` (`/` 200, `/api/health` 200, `POST /api/chat` 200 SSE) →
`theokit build` / `theokit start` 200.

## Test state on develop

- Unit: 3006 passed / 7 skipped / 0 failed
- Integration: 595 passed / 0 failed / 6 skipped
- @theokit/http 395 · @theokit/agents 239 · create-theokit 77 · `pnpm typecheck` 0 errors

## Remaining steps (post human merge)

Re-invoke `/release --resume 8` after the PR merges to:
1. Tag the merge commit `v0.6.0` (annotated) + push.
2. `gh release create v0.6.0`.
3. Update this record to `RELEASED` with merge SHA + release URL.

Plan `milestone_id` is `null` (ad-hoc / off-roadmap) → no ROADMAP.md checkbox flip.
