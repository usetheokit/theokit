# Release theokit@0.15.0 + @theokit/agents@0.30.0

**Date:** 2026-07-05
**Verdict:** RELEASED
**Milestones:** M4 (cohesive harness) + M5 (terminal harness)
**Feature PR:** https://github.com/usetheodev/theokit/pull/72 (merged, sha 2a740f5)
**Version Packages PR:** https://github.com/usetheodev/theokit/pull/73 (merged, sha ad7732c)
**Published:**
- `theokit@0.15.0` (was 0.14.0 — minor)
- `@theokit/agents@0.30.0` (was 0.29.0 — minor)

## Scope

- **M4 — Cohesive harness (Eixo C):** `@HumanInTheLoop` + `@Checkpoint` made functional as an adapter
  over `@theokit/sdk` (ADR 0038). HITL pause via the SDK's `pre_tool_call` hook + `data-checkpoint`
  resume via `FileSystemConversationStorage`. `examples/agent-saas` + deterministic E2E + invariant guard.
- **M5 — Terminal harness (Eixo D):** `theokit agent <name> "<message>"` runs a local agent in the
  terminal (stream + tool cards + `y/N` approval prompt), reusing the M4 harness with a Node-stdlib
  render surface — no new runtime, no TUI dependency (ADR 0039).

## Review

Multi-agent panel (cto-architect + code-reviewer + guardian). Both milestones: HIGH findings fixed,
cto re-review READY_TO_MERGE. No parallel runtime (G2 invariant guards enforce).

## Post-release

ROADMAP M4 + M5 flipped `[x]` — M0–M5 all done. M6 (Dogfood + V1 ship) is unblocked.
