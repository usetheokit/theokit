# Implementation: Close the 17 cross-validation gaps and adopt them in TheoCode

> **Generated by `/implement crossval-4-6-absorption`** — the halt-loop's working contract derived from `.claude/knowledge-base/plans/crossval-4-6-absorption-plan.md`. The plan is the contract; this file is the execution state. Revisions to intent go through `cycle-plan`, never here.

**Slug:** `crossval-4-6-absorption`
**Source plan:** `.claude/knowledge-base/plans/crossval-4-6-absorption-plan.md` (v1.2)
**Plan verdict:** `SHIPPABLE_WITH_CAVEATS` (74.0 — zero hard caps, zero soft caps; the sole detractor is a spec-smell false-positive class documented in the `/plan-confidence` run)
**Working branch:** `workspace` (Unbreakable Rule 4)
**Started:** 2026-08-16

## Pre-condition audit (verified at /implement start)

- [x] Plan verdict ≥ SHIPPABLE_WITH_CAVEATS — 74.0, `hard_caps_triggered: []`
- [x] On `workspace` — `git branch --show-current` → `workspace`
- [x] No uncommitted changes at start — `git status --porcelain` → 0 lines
- [x] Toolchain ready — `package.json` present, node `v22.22.2`, pnpm `9.15.0`
- [x] Runtime version lock satisfied — `.nvmrc` = `22`, `engines.node` = `>=22.12.0`, active `v22.22.2`
- [x] TDD shape gate — `check_tdd_shape.py` → `total_tasks: 23`, `tasks_with_shape: 23`, `blocked_task_ids: []`, `all_pass: true`

## Blocking questions — RESOLVED at /implement start (2026-08-16)

The plan declared two Unresolved Questions as blocking specific tasks. Both were put to the owner **before** the halt-loop started, because an autonomous loop cannot ask mid-flight and one of them decided whether ~190 LOC would be written or discarded. **Neither answer required editing the plan** — both selected a branch the plan had already written.

### Q3 → `assertSecureModes`: **tighten directory creation to 0700**

The owner chose the stronger guarantee over the consumer's measured relaxation. `& 0o022` **stays** on the check; the framework stops creating its credential home group-writable.

Consequences the halt-loop MUST honor in **T2.4**:

- Every path that creates the credential home passes `mode: 0o700` — not only the one the test happens to exercise. A single unfixed creator makes the check and the creator disagree in the *other* direction, which is the same defect one step over.
- A **pre-existing 0775 directory must have a way forward**: repair it, or refuse while naming the exact repair command. Refusing with no path forward is precisely what U-4 caused and what this task exists to end.
- The plan's T2.4 already wrote this branch ("If tightening, add the repair path for pre-existing directories") and its RED test `pre_existing_wrong_mode_is_repaired_or_named()` is marked "only when tightening" — it is now **required**, not optional.
- The security reasoning goes in the module docstring: this is a *tightening*, and the machines it affects are the ones the framework already ran on.

### Q2 → `encodeProjectDir`: **keep lossy, absorb the oracle**

T3.2 proceeds exactly as planned. The reversible-encode alternative was considered and deferred — it is a breaking change to an on-disk layout with live transcripts, and coupling it to a gap-closing plan would join two risks. It stays recorded in the plan's Unresolved Questions as the better long-term fix.

Consequences for **T3.2**: task step 1 ("Confirm Q2's answer — blocking; if reversible, stop") is **satisfied, proceed**. Nothing else changes.

## Task list (derived from plan; ordered by the plan's Dependency Graph)

Phase 0 blocks Phase 4 (T4.1 imports T0.1's `declaredExports()`). Phases 1, 2 and 3 are mutually parallel — disjoint files across three repos. **T5.0 is a hard barrier**: no Phase 5 task may begin before it verifies every expected version is served by the registry.

| # | Plan ref | Status | Wiring (a) | Wiring (b) | Wiring (c) | Commit SHA |
|---|---|---|---|---|---|---|
| T0.1 | Phase 0 / T0.1 | pending | — | — | — | — |
| T1.1 | Phase 1 / T1.1 | pending | — | — | — | — |
| T1.2 | Phase 1 / T1.2 | pending | — | — | — | — |
| T1.3 | Phase 1 / T1.3 | pending | — | — | — | — |
| T2.1 | Phase 2 / T2.1 | pending | — | — | — | — |
| T2.2 | Phase 2 / T2.2 | pending | — | — | — | — |
| T2.3 | Phase 2 / T2.3 | pending | — | — | — | — |
| T2.4 | Phase 2 / T2.4 | pending | — | — | — | — |
| T2.5 | Phase 2 / T2.5 | pending | — | — | — | — |
| T2.6 | Phase 2 / T2.6 | pending | — | — | — | — |
| T2.7 | Phase 2 / T2.7 | pending | — | — | — | — |
| T3.1 | Phase 3 / T3.1 | pending | — | — | — | — |
| T3.2 | Phase 3 / T3.2 | pending | — | — | — | — |
| T3.3 | Phase 3 / T3.3 | pending | — | — | — | — |
| T3.4 | Phase 3 / T3.4 | pending | — | — | — | — |
| T4.1 | Phase 4 / T4.1 | pending | — | — | — | — |
| T4.2 | Phase 4 / T4.2 | pending | — | — | — | — |
| T4.3 | Phase 4 / T4.3 | pending | — | — | — | — |
| T5.0 | Phase 5 / T5.0 | pending | n/a | n/a | n/a | — |
| T5.1 | Phase 5 / T5.1 | pending | — | — | — | — |
| T5.2 | Phase 5 / T5.2 | pending | — | — | — | — |
| T5.3 | Phase 5 / T5.3 | pending | — | — | — | — |
| T5.4 | Phase 5 / T5.4 | pending | — | — | — | — |

Status legend: `pending` / `red` / `green` / `refactor` / `wired` / `committed` / `blocked` / `done`
Wiring legend: `✓` pillar passed · `✗` pillar failed · `defer` ADR-deferred · `—` not yet checked · `n/a` task produces no symbol.

## Cross-repo note

Four repositories move in this plan. Each has its own `workspace` branch, its own suite and its own release:

| Repo | Path | Tasks |
|---|---|---|
| `theokit` | `.` | T0.1, T1.1, T1.2, T2.1, T2.2, T2.3, T2.6, T2.7, T3.2, T3.3, T4.1, T4.2, T4.3, T5.4 |
| `theokit-tui` | `../theokit-tui` | T3.1, T3.4 |
| `theokit-sdk` | `../theokit-sdk` | T1.3, T2.4, T2.5 |
| `TheoCode` | `../../usetheo-labs/TheoCode` | T5.1, T5.2, T5.3 |

Wiring pillar (a) is evaluated **per repo**: a symbol shipped in `theokit-tui` whose only caller lives in `TheoCode` cannot satisfy pillar (a) inside `theokit-tui` until the adoption phase. Where that happens, the task records `defer` with the adopting task named — never a no-op caller, which is the documented gaming anti-pattern.

## Per-task contract

The halt-loop fills each task's detail progressively as it iterates. Every task honors the `cycle-implement.md` hard gates: RED must fail before GREEN, parsimony ladder walked before GREEN code, REFACTOR against SOLID/Clean Code/DRY, wiring triad before commit, atomic conventional commit referencing the task id.

Per-iteration audit trail is written to `.claude/knowledge-base/implementations/.progress-crossval-4-6-absorption.json` (schema: `skills/implement/templates/progress-schema.json`).
