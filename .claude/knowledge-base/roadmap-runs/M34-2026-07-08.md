---
milestone_id: M34
slug: mcp-surface-hardening
date: 2026-07-08
status: completed
checkbox_flipped_at: 2026-07-08
closes: "#97"
---

# Milestone M34 — Phase 2: MCP surface hardening + default-DENY

## Outcome
MCP is now a real, secured framework-core surface. tools/call EXECUTES the tool (via the compiled
tools with real inputSchema + handler; returns CallToolResult); tools/list retains the real Zod
inputSchema (was dropped); protocol bumped 2024-11-05 → 2025-06-18. MCP route auth gate closes #97
(CSRF parity with the agent-run route, threaded csrfMode from dev + prod callers) + DEFAULT-DENY
(explicit `export const mcp = true` opt-in; auto-mount removed — breaking, documented in CHANGELOG
§ Security). 42 MCP tests green, tsc 0 errors, regression 26/26. #97 verified-and-closed.
