---
milestone_id: M33
slug: typed-ctx-inprocess-caller
date: 2026-07-08
status: completed
checkbox_flipped_at: 2026-07-08
files: [packages/theo/src/server/http/in-process-caller.ts, packages/theo/src/server/http/validate-route-input.ts, packages/theo/src/server/http/ctx-reconciliation.ts, tests/unit/in-process-caller.test.ts, tests/integration/in-process-vs-http-parity.test.ts, tests/ctx-reconciliation.test-d.ts]
---

# Milestone M33 — Phase 1: Typed-ctx reconciliation + in-process caller

## Outcome
Shipped `callProcedure` (the in-process typed caller the TUI/Tauri/MCP surfaces need — invoke route
logic without an HTTP Request), sharing the ONE Zod validation pipeline with the HTTP path
(`validateRouteInput`, extracted; HTTP↔in-process parity test green). Plus the ctx reconciliation
contract: typed TCtx = the context.ts factory value only; plugin decorations + jobBackend ctx.queue
(the other 2 execute.ts writers) are not typed onto the route surface (queue via opt-in
JobsAugmentedCtx) — type-tested against execute.ts, 5-arity generic (GAP-4) preserved. 42+ tests
green, tsc 0 errors, lint clean. Additive.
