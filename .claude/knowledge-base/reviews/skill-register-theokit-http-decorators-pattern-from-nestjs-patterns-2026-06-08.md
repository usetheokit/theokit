# Skill Registration: theokit-http-decorators-pattern-from-nestjs-patterns

**Date:** 2026-06-08
**Source blueprint:** theokit-http-decorators-pattern-from-nestjs
**Blueprint verdict at generation:** SHIPPABLE_WITH_CAVEATS
**Blueprint score at generation:** 89.0
**Validator verdict:** PASS (verified at registration time)
**Registered from:** .claude/skills/generated/theokit-http-decorators-pattern-from-nestjs-patterns/
**Registered to:** .claude/skills/theokit-http-decorators-pattern-from-nestjs-patterns/

## Skill description (verbatim from frontmatter)

Use when /to-plan targets `@theokit/http-decorators` v0.1.0 implementation. Use when designing NestJS-style decorator bridges on top of `defineRoute` in src/server/define/. Use when deciding between Legacy `experimentalDecorators` vs TC39 Stage-3 decorators in TheoKit packages. Use when wiring `@UseGuards` / `@UseInterceptors` to TheoKit `defineMiddleware`. Use when extending `theokit generate` with a `controller` verb in src/cli/commands/generate.ts. Use when bridging DTO classes to Zod schemas while preserving the "Zod is the Single Source of Truth" invariant from .claude/rules/type-safety.md.

## Audit chain

This skill was generated by `/skill-writer` from a `/discover-execute` blueprint, passed `/skill-validator` checks (frontmatter + citations + no-duplication + description-clarity + no-forbidden-patterns), and was promoted by `/skill-register` on the date above.

The `.source-blueprint` marker file inside the skill dir preserves the audit chain after registration.

## How /to-plan now consumes this skill

Step 0 of `/to-plan` scans `.claude/skills/*/SKILL.md` frontmatter `description` fields for relevant trigger phrases. When the user invokes `/to-plan` with a topic that matches the trigger phrases above, this skill is loaded into context.

To verify: invoke `/to-plan` with a prompt matching the trigger phrases. The plan should cite the Patterns / Recommendations from this skill.

## Rollback

```
mv .claude/skills/theokit-http-decorators-pattern-from-nestjs-patterns/ .claude/skills/generated/theokit-http-decorators-pattern-from-nestjs-patterns/
# Optionally delete this audit entry.
# Optionally revert any .claude/settings.json patches (none expected for read-only skills).
```

