# Dogfood Audit — 2026-07-05

**Anchor:** `agent-chat-new-surface`
**Mode:** audit

```
EVIDENCE_SUFFICIENT
```

## Hard caps (all pass)

| # | Check | Result |
|---|---|---|
| 1 | Anchor section + `Status` present in manifest | ✅ `Slug: agent-chat-new-surface`, `Status: running` |
| 2 | `Status` = `running` | ✅ |
| 3 | ≥ 1 evidence file with `scenario: agent-chat-new-surface` | ✅ 3 files |
| 4 | Most recent evidence within 30 days | ✅ all dated 2026-07-05 (age 0) |

## Soft caps (all pass)

| Soft cap | Threshold | Result |
|---|---|---|
| Total evidence count | ≥ 3 | ✅ 3 |
| Failure stories present | ≥ 1 | ✅ 1 (`…-054500.md`, `outcome: fail`) |
| Distinct operators | ≥ 1 (single-maintainer tuning) | ✅ 1 (maintainer) |

## Evidence

| File | Outcome | Summary |
|---|---|---|
| `…-054500.md` | fail | The first live dogfood surfaced 2 real v1 bugs (stale `@theokit/sdk` pin; tool-call double-wrap) — both fixed (`2302dcb`). |
| `…-060000.md` | pass | Fresh `npx create-theokit` app streamed a real chat response over `UIMessageStream` via OpenRouter. |
| `…-061500.md` | pass | A real tool call ran end-to-end (`add 137+456 → 593`) on the fixed build via OpenRouter. |

## Verdict

**`EVIDENCE_SUFFICIENT`** — the v1.0 claim for the `agent-chat-new-surface` anchor is honestly backed:
a freshly scaffolded app streams a real chat and runs a real tool call against a real model on the
maintainer's own infrastructure. The dogfood also did its job — it caught (and the team fixed) two
real v1 bugs that stubbed tests missed. The tool-call fix ships to users with the `theokit@0.15.1`
release (the live evidence used the fixed build via `pnpm.overrides`; the failure evidence documents
the still-published pre-fix state honestly).
