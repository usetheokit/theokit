import { defineConfig } from 'theokit'
// Integrations array — experimental in alpha. Wiring is via `theoPlugin`
// options at the Vite plugin layer for now. See ./integrations/banner.ts
// and the README for the shape and the canonical registration call site.
// import banner from './integrations/banner.js'
// integrations: [banner]

export default defineConfig({})
